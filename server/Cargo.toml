[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
rusqlite = { version = "0.32.0", features = ["bundled"] }
lingua = { version = "1.7.1", default-features = false, features = ["english","spanish"] }
